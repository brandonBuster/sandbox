{
    "name": "Cloud Dev Env",
    "build": {
        "dockerfile": "Dockerfile"   
    },
    "customizations": {
        "vscode": {
            "settings": {
                "terminal.integrated.defaultProfile.linux": "bash"
            }
        }
    },
    "postCreateCommand": "terraform -install-autocomplete && curl -Ls https://astral.sh/uv/install.sh | bash",
    "remoteUser": "vscode",
    "features": {}
}